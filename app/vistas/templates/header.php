<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.8/css/materialize.min.css">
    <link rel="stylesheet" href="css/bootstrap.min.css">

    <script src="js/jquery.js"></script>
<!--    <script src="js/bootstrap.js"></script>-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.8/js/materialize.min.js"></script>


</head>
<body>

<?php

/*
 *
 *

    <style>
/*        .vert-center {
            display: flex;
            justify-content: center;
            align-items: center;
            ;
        }

        .vh-100 {
            min-height: 100vh;
            margin-top: 35px
        }

        #beepLogo {
            width: 48px;
        }
       header {
            padding: 12px;
            !*background: rgba(100, 100, 100, 1);*!
        }

        .white {
            color: #ffffff;
        }

        .topRow {
            display: flex;
            flex-direction: row;
            flex-wrap: nowrap;
        }

        .logo {
            position: relative;
            !*top: 4px;*!
            flex-grow: 1;
            text-align: center;
        }

        .menuicon {

        }

        .menu {
            width: 90%;
            max-width: 450px;
            height: 100vh;
            background: white;
            box-shadow: 5px 0 12px rgba(0, 0, 0, 0.3);
            position: absolute;
            top: 0;
            left: 0;
            will-change: transform;
            transform: translate(0%, 0);
            z-index: 20;
            transition: 250ms cubic-bezier(0, 0, 0.31, 1);
        }

        #menuHeader {
            padding: 12px;
            background: #e61f2a;
            color: #fff;
        }

        .bg-principal {
            background: #e61f2a !important;
            color: white !important;
        }

        .bg-rosa {
            background: #f8f2f2;
            color: rgba(0, 0, 0, 0.8) !important;
        }

!*
        .overlaySidebar {
            content: '';
            height: 100vh;;
            width: 100vw;
            background: rgba(0, 0, 0, 0.5);
            position: absolute;
            top: 0;
            left: 0;
            will-change: transform;
            z-index: 1;
            opacity: 1;
            transition: 250ms cubic-bezier(0, 0, 0.31, 1);
        }

        !*.container {*!
        !*background: #aaa;*!
        !*width: 100vw;*!
        !*height: 100%;*!
        !*}*!

        .sidebar.hidden .menu {
            transform: translate(-102%, 0);
            !*left: -102vw;*!
        }

        .sidebar.hidden .overlaySidebar {
            opacity: 0;
            pointer-events: none;
        }
*!

        #menuContent {
            padding: 0;
            margin: 0;
        }

        #menuOptions {
            padding: 0;
            margin: 0;
            list-style: none;
            display: flex;
            flex-direction: column;
        }

        #menuOptions li {
            background: #eee;
        }

        #slideMenu {
            position: absolute;
            top: 0;
            left: 0;
            height: 100vh;
            width: 12px;
            z-index: 999;
        }

        .font-large {
            font-size: large;
        }

        .font-larger {
            font-size: larger;
        }

        #topBar {
            position: absolute;
            top: 0%;
            left: 0%;
            width: 100vw;
        }

    </style>



 *  * */